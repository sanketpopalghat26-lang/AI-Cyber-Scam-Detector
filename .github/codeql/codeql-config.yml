# =============================================================================
# CodeQL Configuration — AI Cyber Scam Detector
# =============================================================================
name: "CodeQL Config"

queries:
  - name: Security and Quality
    uses: security-and-quality

paths:
  - backend
  - frontend/src
  - enterprise_pipeline

paths-ignore:
  - '**/node_modules/**'
  - '**/dist/**'
  - '**/build/**'
  - '**/*.min.js'
  - 'backend/alembic/versions'
